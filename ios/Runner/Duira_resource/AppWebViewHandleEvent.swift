
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let data_sTransformPath:[Character] = ["g","e","t","D","e","v"]
fileprivate let noti_tempKey:String = "iceIDminimum large ting"

/*: "getFirebaseID" :*/
fileprivate let loggerMinimumEmptyMessage:[Character] = ["g","e","t","F","i","r","e"]
fileprivate let kComponentKey:String = "B"
fileprivate let controllerClearDate:String = "camera commandaseID"

/*: "getAreaISO" :*/
fileprivate let main_cornerPath:String = "getAreaday map add fire device"
fileprivate let configDayURL:[Character] = ["I","S","O"]

/*: "getProxyStatus" :*/
fileprivate let cacheZonePackStr:String = "in prompt total string closegetP"
fileprivate let constHumanActivityValue:String = "Statfalse domain result"
fileprivate let show_contentTargetResult:String = "urun"

/*: "getMicStatus" :*/
fileprivate let data_pleaseURL:String = "mirror pic okgetMi"
fileprivate let main_manageName:String = "service"

/*: "getPhotoStatus" :*/
fileprivate let factorySuccessURL:String = "valid break by guard centergetPho"
fileprivate let networkMapId:String = "system in networktoStatus"

/*: "getCameraStatus" :*/
fileprivate let user_removeAppData:[Character] = ["g","e","t","C","a","m","e","r","a","S","t","a","t"]
fileprivate let parserPathString:String = "uend"

/*: "reportAdjust" :*/
fileprivate let configKitForgetKey:[Character] = ["r","e","p","o","r","t","A","d","j","u","s","t"]

/*: "requestLocalPush" :*/
fileprivate let showSpaceTingURL:String = "requestLpanel on of"
fileprivate let helperValidKey:[Character] = ["o","c","a","l","P","u","s","h"]

/*: "getLangCode" :*/
fileprivate let controllerDeviceAgentFlag:[Character] = ["g","e","t","L","a","n","g","C"]
fileprivate let factoryRunningStatus:[Character] = ["o","d","e"]

/*: "getTimeZone" :*/
fileprivate let mainAdjustScreenList:String = "getTforget appear class decide begin"
fileprivate let serviceBounceTime:String = "nlocal"

/*: "getInstalledApps" :*/
fileprivate let user_enterTitle:String = "getIdecision go"
fileprivate let configUnfinishedUpResult:String = "visible coloredApps"

/*: "getSystemUUID" :*/
fileprivate let formatterFlagState:String = "geinput"
fileprivate let data_hideError:String = "revenue float name tapSystem"

/*: "getCountryCode" :*/
fileprivate let appZoneTitle:String = "getCoufloat presentation sum"
fileprivate let loggerAppearCalendarSecret:[Character] = ["n"]
fileprivate let const_methodPath:[Character] = ["t","r","y","C","o","d","e"]

/*: "inAppRating" :*/
fileprivate let showPersistState:String = "inApkit visible observer"

/*: "apPay" :*/
fileprivate let data_startList:String = "tab boundaryapPay"

/*: "subscribe" :*/
fileprivate let networkHeadReportFormat:[Character] = ["s","u","b","s","c","r"]
fileprivate let managerTimeName:String = "radiobe"

/*: "openSystemBrowser" :*/
fileprivate let userHideLanguageID:String = "openSystarrow second application copy"
fileprivate let dataBlackName:[Character] = ["e","m","B","r","o","w","s","e","r"]

/*: "closeWebview" :*/
fileprivate let user_needDict:[Character] = ["c","l","o","s","e"]
fileprivate let modelBondSecret:String = "frame triggerWebview"

/*: "openNewWebview" :*/
fileprivate let configSoundLaterData:String = "any other i tryopenNe"
fileprivate let cacheProduceImageId:String = "wWebviewback by image start"

/*: "reloadWebview" :*/
fileprivate let routerBackVersion:[Character] = ["r","e","l","o","a","d","W","e","b","v","i"]
fileprivate let routerBounceControlPath:String = "echoice"

/*: "openSettings" :*/
fileprivate let user_kitKey:[Character] = ["o","p","e","n","S","e","t","t","i","n","g"]
fileprivate let networkPurchasePath:[Character] = ["s"]

/*: "getNotificationStatus" :*/
fileprivate let parserLogPath:String = "getNotok space"
fileprivate let main_unfinishedFormat:String = "album handle minute copy managerionStatus"

/*: "setScheduledLocalPush" :*/
fileprivate let cacheFomiteKey:String = "SE"
fileprivate let noti_ratingAdvanceDate:[Character] = ["t","S","c","h","e","d","u","l","e","d","L","o","c","a","l","P","u","s","h"]

/*: "typeName" :*/
fileprivate let const_displaySecret:[UInt8] = [0xf9,0xfe,0xf5,0xea,0xd3,0xe6,0xf2,0xea]

fileprivate func presentationPush(automatic num: UInt8) -> UInt8 {
    let value = Int(num) - 133
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "deviceID" :*/
fileprivate let factoryLethalList:[UInt8] = [0x17,0x16,0x5,0x1a,0x10,0x16,0x3a,0x37]

private func arrayNever(go num: UInt8) -> UInt8 {
    return num ^ 115
}

/*: "fireBaseID" :*/
fileprivate let cachePoorName:[UInt8] = [0x38,0x3b,0x44,0x37,0x14,0x33,0x45,0x37,0x1b,0x16]

fileprivate func networkLine(normal num: UInt8) -> UInt8 {
    let value = Int(num) - 210
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "areaISO" :*/
fileprivate let parserFrameSecret:[UInt8] = [0x78,0x6b,0x7c,0x78,0x50,0x4a,0x56]

private func sceneExecute(fatal num: UInt8) -> UInt8 {
    return num ^ 25
}

/*: "isProxy" :*/
fileprivate let cacheOptionMessage:[UInt8] = [0xc8,0xd2,0xf1,0xd3,0xce,0xd9,0xd8]

private func largeTap(progress num: UInt8) -> UInt8 {
    return num ^ 161
}

/*: "langCode" :*/
fileprivate let mainTravelKitID:[UInt8] = [0x56,0x5b,0x54,0x5d,0x79,0x55,0x5e,0x5f]

private func territorialDivisionAccounting(close num: UInt8) -> UInt8 {
    return num ^ 58
}

/*: "timeZone" :*/
fileprivate let k_footingToken:[UInt8] = [0xa8,0xb5,0xb1,0xb9,0x86,0xb3,0xb2,0xb9]

private func reportScreen(going num: UInt8) -> UInt8 {
    return num ^ 220
}

/*: "installedApps" :*/
fileprivate let controllerFactoryUrl:[UInt8] = [0x60,0x65,0x6a,0x6b,0x58,0x63,0x63,0x5c,0x5b,0x38,0x67,0x67,0x6a]

fileprivate func wrongdoingReceive(automatic num: UInt8) -> UInt8 {
    let value = Int(num) + 9
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "systemUUID" :*/
fileprivate let minimumPromptTime:[UInt8] = [0xde,0xd4,0xde,0xd9,0xc8,0xc0,0xf8,0xf8,0xe4,0xe9]

private func sessionKey(count num: UInt8) -> UInt8 {
    return num ^ 173
}

/*: "countryCode" :*/
fileprivate let modelFilterVersion:[UInt8] = [0x86,0x8a,0x90,0x8b,0x91,0x97,0x9c,0xa6,0x8a,0x81,0x80]

/*: "status" :*/
fileprivate let modelOkCancelStatus:[UInt8] = [0x68,0x6f,0x7a,0x6f,0x6e,0x68]

/*: "isAuth" :*/
fileprivate let managerBlackError:[UInt8] = [0x6f,0x75,0x47,0x73,0x72,0x6e]

/*: "isFirst" :*/
fileprivate let appTrackToken:[UInt8] = [0x26,0x3c,0x9,0x26,0x3d,0x3c,0x3b]

private func dateSession(shared num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: __LocalPush" :*/
fileprivate let engineVersionMode:[Character] = ["_","_","L","o","c","a","l"]
fileprivate let k_topURL:[Character] = ["P","u","s","h"]

/*: "identifier" :*/
fileprivate let engineInformationSystemURL:[UInt8] = [0x1c,0x17,0x18,0x21,0x27,0x1c,0x19,0x1c,0x18,0x25]

fileprivate func permissionFinish(establish num: UInt8) -> UInt8 {
    let value = Int(num) + 77
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "HTTPProxy" :*/
fileprivate let factoryDeviceFlag:String = "HokP"
fileprivate let user_endKey:String = "Proxyof open"

/*: "HTTPSProxy" :*/
fileprivate let configActivityFlag:[Character] = ["H","T","T","P","S","P"]
fileprivate let sessionReceiveDate:[Character] = ["r","o","x","y"]

/*: "SOCKSProxy" :*/
fileprivate let constTunKey:[Character] = ["S","O","C","K","S","P"]
fileprivate let networkActivityStatus:String = "roxpurchase"

/*: "__SCOPED__" :*/
fileprivate let main_presentationTime:String = "challenge minute window__SC"

/*: "tap" :*/
fileprivate let viewChoiceMessage:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let formatterCalendarStatus:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let data_appearQueryString:String = "ipsepresent"

/*: "ppp" :*/
fileprivate let sessionAccessUrl:String = "hourp"

/*: "Retry After or Go to " :*/
fileprivate let k_protectionCount:String = "along scheme category failRetry Aft"
fileprivate let parserOriginExecuteProcessingMessage:String = "factory frameer "
fileprivate let mainTransformHideMessage:[Character] = ["o","r"," ","G","o"," ","t","o"," "]

/*: "Feedback" :*/
fileprivate let engineToolValue:String = "Feedbacklarge monitor do"

/*: " to contact us" :*/
fileprivate let mainTheMState:[Character] = [" ","t","o"," ","c"]
fileprivate let noti_executeDict:String = "onstylea"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppWebViewHandleEvent.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let routerAvailableReplaceVersion     = (String(data_sTransformPath) + String(noti_tempKey.prefix(5)))        // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let dataLiveBuildProgressMsg   = (String(loggerMinimumEmptyMessage) + kComponentKey.lowercased() + String(controllerClearDate.suffix(5)))      // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let managerTimeStatus      = (String(main_cornerPath.prefix(7)) + String(configDayURL))         // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let notiCameraTagFormat  = (String(cacheZonePackStr.suffix(4)) + "roxy" + String(constHumanActivityValue.prefix(4)) + show_contentTargetResult.replacingOccurrences(of: "run", with: "s"))     // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let k_forgetKey    = (String(data_pleaseURL.suffix(5)) + "cStatu" + main_manageName.replacingOccurrences(of: "service", with: "s"))       // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let data_trustTitle  = (String(factorySuccessURL.suffix(6)) + String(networkMapId.suffix(8)))     // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let showSupplantMessage = (String(user_removeAppData) + parserPathString.replacingOccurrences(of: "end", with: "s"))    // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let showFormerCount    = (String(configKitForgetKey))       // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let managerPersistMessage = (String(showSpaceTingURL.prefix(8)) + String(helperValidKey))  // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let constPreviseUrl      = (String(controllerDeviceAgentFlag) + String(factoryRunningStatus))        // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let environmentPhotoToken      = (String(mainAdjustScreenList.prefix(4)) + "imeZo" + serviceBounceTime.replacingOccurrences(of: "local", with: "e"))        // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let noti_trustString = (String(user_enterTitle.prefix(4)) + "nstall" + String(configUnfinishedUpResult.suffix(6)))   // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let const_arraySystemToken    = (formatterFlagState.replacingOccurrences(of: "input", with: "t") + String(data_hideError.suffix(6)) + "UUID")      // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let cacheOutlineTotalryDict   = (String(appZoneTitle.prefix(6)) + String(loggerAppearCalendarSecret) + String(const_methodPath))     // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let networkMonitorReplaceMessage      = (String(showPersistState.prefix(4)) + "pRating")        // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let parserSumId            = (String(data_startList.suffix(5)))              // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let networkTransformResult        = (String(networkHeadReportFormat) + managerTimeName.replacingOccurrences(of: "radio", with: "i"))          // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let mainGenerateLocalDict = (String(userHideLanguageID.prefix(8)) + String(dataBlackName)) // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let k_radioEventString     = (String(user_needDict) + String(modelBondSecret.suffix(7)))       // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let configDisappearUrl   = (String(configSoundLaterData.suffix(6)) + String(cacheProduceImageId.prefix(8)))     // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let serviceChangeStatus    = (String(routerBackVersion) + routerBounceControlPath.replacingOccurrences(of: "choice", with: "w"))      // 重载webView
//: private let openSettings = "openSettings"                   // v2.0.4新增：打开通知设置页
private let show_penaliseStr = (String(user_kitKey) + String(networkPurchasePath))                   // v2.0.4新增：打开通知设置页
//: private let getNotificationStatus = "getNotificationStatus" // v2.0.4新增：获取通知权限
private let parserServerZoneKey = (String(parserLogPath.prefix(6)) + "ificat" + String(main_unfinishedFormat.suffix(9))) // v2.0.4新增：获取通知权限
//: private let setScheduledLocalPush = "setScheduledLocalPush" // v2.0.5新增：设置定时本地消息推送
private let kUsedOpenName = (cacheFomiteKey.lowercased() + String(noti_ratingAdvanceDate)) // v2.0.5新增：设置定时本地消息推送

//: struct JSMessageModel: HandyJSON {
struct SlopPailOpen: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: UsedSecond?
    // 内购/订阅 H5参数
    //: var goodsId: String?     
    var goodsId: String?     // 商品Id
    //: var source: Int?         
    var source: Int?         // 充值来源
    //: var type: Int?           
    var type: Int?           // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?         
    var url: String?         // url
    //: var fullscreen: Int?     
    var fullscreen: Int?     // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?   
    var transparency: Int?   // transparency：0-webview白色背景 1-webview透明背景
    //: var time: [Int]?         
    var time: [Int]?         // 本地推送当天时间（24小时制）
    //: var msg: [String]?       
    var msg: [String]?       // 本地推送文案
}

//: struct UserInfoModel: HandyJSON {
struct UsedSecond: HandyJSON {
    //: var headPic: String?   
    var headPic: String?   // 头像
    //: var nickname: String?  
    var nickname: String?  // 昵称
    //: var uid: String?       
    var uid: String?       // 用户Id
}

//: extension AppWebViewController {
extension PropertyViewController {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func user(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = SlopPailOpen.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case routerAvailableReplaceVersion:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = RevenueVertical.can()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: factoryLethalList.map{arrayNever(go: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case dataLiveBuildProgressMsg:
                //: AppWebViewController.getFireBaseToken { str in
                PropertyViewController.allContent { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: cachePoorName.map{networkLine(normal: $0)}, encoding: .utf8)!: str])
                }
                
            //: case getAreaISO:
            case managerTimeStatus:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = PropertyViewController.isCurrent()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: parserFrameSecret.map{sceneExecute(fatal: $0)}, encoding: .utf8)!: arr.joined(separator: ",")])
                
            //: case getProxyStatus:
            case notiCameraTagFormat:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = PropertyViewController.chromaticColorTransaction()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: cacheOptionMessage.map{largeTap(progress: $0)}, encoding: .utf8)!: status])
              
            //: case getLangCode:
            case constPreviseUrl:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: mainTravelKitID.map{territorialDivisionAccounting(close: $0)}, encoding: .utf8)!: langCode])
                
            //: case getTimeZone:
            case environmentPhotoToken:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: k_footingToken.map{reportScreen(going: $0)}, encoding: .utf8)!: timeZone])
                
            //: case getInstalledApps:
            case noti_trustString:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: controllerFactoryUrl.map{wrongdoingReceive(automatic: $0)}, encoding: .utf8)!: apps])
                
            //: case getSystemUUID:
            case const_arraySystemToken:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: minimumPromptTime.map{sessionKey(count: $0)}, encoding: .utf8)!: uuid])
                
            //: case getCountryCode:
            case cacheOutlineTotalryDict:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: modelFilterVersion.map{$0^229}, encoding: .utf8)!: countryCode])
                
            //: case inAppRating:
            case networkMonitorReplaceMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                PropertyViewController.inwards()

            //: case apPay:
            case parserSumId:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.bar(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: modelOkCancelStatus.map{$0^27}, encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case networkTransformResult:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.bar(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: modelOkCancelStatus.map{$0^27}, encoding: .utf8)!: success])
                    }
                }
                
            //: case openSystemBrowser:
            case mainGenerateLocalDict:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }
                
            //: case closeWebview:
            case k_radioEventString:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.pack()
                
            //: case openNewWebview:
            case configDisappearUrl:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                    //: let transparency = model.transparency,
                    let transparency = model.transparency,
                    //: let fullscreen = model.fullscreen {
                    let fullscreen = model.fullscreen {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    PropertyViewController.decision(urlStr, transparency, fullscreen)
                }
                
            //: case reloadWebview:
            case serviceChangeStatus:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.rating()
            
            //: case openSettings:
            case show_penaliseStr:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName])
                //: if let url = URL(string: UIApplication.openSettingsURLString) {
                if let url = URL(string: UIApplication.openSettingsURLString) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                    UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                }
                
            //: case setScheduledLocalPush:
            case kUsedOpenName:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName])
                //: LocalPushScheduler.shared.schedule(times: model.time ?? [], contents: model.msg ?? [])
                AlreadyAgent.shared.timeThroughProgress(times: model.time ?? [], contents: model.msg ?? [])
                
            //: case getNotificationStatus:
            case parserServerZoneKey:
                //: AppPermissionTool.shared.requestNotificationPermission { auth, isFirst in
                ConvertQuantity.shared.deviceForMethod { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: managerBlackError.map{$0^6}, encoding: .utf8)!: auth, String(bytes: appTrackToken.map{dateSession(shared: $0)}, encoding: .utf8)!: isFirst])
                }
            
            //: case getMicStatus:
            case k_forgetKey:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                ConvertQuantity.shared.decideBack { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: managerBlackError.map{$0^6}, encoding: .utf8)!: auth, String(bytes: appTrackToken.map{dateSession(shared: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case getPhotoStatus:
            case data_trustTitle:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                ConvertQuantity.shared.messageToRange { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: managerBlackError.map{$0^6}, encoding: .utf8)!: auth, String(bytes: appTrackToken.map{dateSession(shared: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case getCameraStatus:
            case showSupplantMessage:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                ConvertQuantity.shared.adjust { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName, String(bytes: managerBlackError.map{$0^6}, encoding: .utf8)!: auth, String(bytes: appTrackToken.map{dateSession(shared: $0)}, encoding: .utf8)!: isFirst])
                }
                
            //: case reportAdjust:
            case showFormerCount:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        RevenueVertical.textError(token: token, count: count)
                    //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        RevenueVertical.on(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case managerPersistMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: const_displaySecret.map{presentationPush(automatic: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                PropertyViewController.accountingData(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event
//: extension AppWebViewController {
extension PropertyViewController {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func decision(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = PropertyViewController()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        MakeTap.mopUp()?.present(vc, animated: true)
    }
    
    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func accountingData(_ model: SlopPailOpen) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()
                
            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(configWarnUsedProgressTitle)" + (String(engineVersionMode) + String(k_topURL))
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: engineInformationSystemURL.map{permissionFinish(establish: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }
                
            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }
    
    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func allContent(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
            //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func isCurrent() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func chromaticColorTransaction() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if PropertyViewController.fromAt() || PropertyViewController.head() {
            //: return true
            return true
        }
        //: return false
        return false
    }
    
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func fromAt() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(factoryDeviceFlag.replacingOccurrences(of: "ok", with: "TT") + String(user_endKey.prefix(5)))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(String(configActivityFlag) + String(sessionReceiveDate))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(String(constTunKey) + networkActivityStatus.replacingOccurrences(of: "purchase", with: "y"))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }
    
    /// 判断用户设备是否开启代理VPN
   /// - Returns: false: 未开启  true: 开启
   //: class func isUsedVPN() -> Bool {
   class func head() -> Bool {
       //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
       guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
       //: guard let dict = proxy as? [String: Any] else { return false }
       guard let dict = proxy as? [String: Any] else { return false }
       //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
       guard let scopedDic = dict[(String(main_presentationTime.suffix(4)) + "OPED__")] as? [String: Any] else { return false }
       //: for keyStr in scopedDic.keys {
       for keyStr in scopedDic.keys {
           //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
           if keyStr.contains((String(viewChoiceMessage))) || keyStr.contains((String(formatterCalendarStatus))) || keyStr.contains((data_appearQueryString.replacingOccurrences(of: "present", with: "c"))) || keyStr.contains((sessionAccessUrl.replacingOccurrences(of: "hour", with: "pp"))){
               //: return true
               return true
           }
       }
       //: return false
       return false
   }
    
    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func inwards() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
        //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }
    
    // MARK: - Event
    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func bar(productId: String, source: Int = -1, payType: MoveDeliveryAccomplishment, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        ProjectMin.event()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        TailorProcess.shared.line(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            ProjectMin.path()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    ProjectMin.purchase( (String(k_protectionCount.suffix(9)) + String(parserOriginExecuteProcessingMessage.suffix(3)) + String(mainTransformHideMessage)) + "\"" + (String(engineToolValue.prefix(8))) + "\"" + (String(mainTheMState) + noti_executeDict.replacingOccurrences(of: "style", with: "t") + "ct us"))
                    
                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.info()
                    
                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}