
//: Declare String Begin

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let cacheHeadString:[UInt8] = [0x2e,0x25,0x29,0x3f,0x27,0x2f,0x24,0x3e,0x64,0x2d,0x2f,0x3e,0xf,0x26,0x2f,0x27,0x2f,0x24,0x3e,0x39,0x8,0x33,0x1e,0x2b,0x2d,0x4,0x2b,0x27,0x2f,0x62,0x6d,0x28,0x25,0x2e,0x33,0x6d,0x63,0x11,0x7a,0x17,0x64,0x39,0x3e,0x33,0x26,0x2f,0x64,0x28,0x2b,0x29,0x21,0x2d,0x38,0x25,0x3f,0x24,0x2e,0x77,0x6d,0x38,0x2d,0x28,0x2b,0x62,0x7a,0x66,0x7a,0x66,0x7a,0x66,0x7a,0x63,0x6d]

/*: "closeWeb" :*/
fileprivate let engineOriginPath:[Character] = ["c","l","o","s","e","W","e","b"]

/*: "toUrl" :*/
fileprivate let mainBlackState:String = "toUrllet local production"

/*: "syncAppInfo" :*/
fileprivate let loggerBackwashPath:[Character] = ["s","y","n","c","A","p","p","I","n","f"]
fileprivate let userHandleMinimumDate:[Character] = ["o"]

/*: "typeName" :*/
fileprivate let factoryLinkRemoteVersion:String = "typeNprivacy page us network"
fileprivate let notiPropertyTitle:String = "netme"

/*: "isAuth" :*/
fileprivate let showUserID:[Character] = ["i","s","A","u","t","h"]

/*: "isFirst" :*/
fileprivate let formatterPageFormat:String = "short action headisFirst"

/*: "Please click 'Go' to allow access" :*/
fileprivate let serviceManagerUrl:[UInt8] = [0x73,0x73,0x65,0x63,0x63,0x61,0x20,0x77,0x6f,0x6c,0x6c,0x61,0x20,0x6f,0x74,0x20,0x27,0x6f,0x47,0x27,0x20,0x6b,0x63,0x69,0x6c,0x63,0x20,0x65,0x73,0x61,0x65,0x6c,0x50]

/*: "getCameraStatus" :*/
fileprivate let modelPadStatus:String = "getCamok component i foundation transaction"
fileprivate let modelChoiceId:[Character] = ["a","t","u","s"]

/*: "Please allow ' :*/
fileprivate let notiApplicationProcessStr:String = "Please alaccomplishment data please scale contact"
fileprivate let showPreviousList:[Character] = ["l","o","w"," ","\'"]

/*: ' to access your camera in your iPhone's 'Settings-Privacy-Camera' option" :*/
fileprivate let controllerOkValue:[UInt8] = [0x92,0x95,0xc1,0xda,0x95,0xd4,0xd6,0xd6,0xd0,0xc6,0xc6,0x95,0xcc,0xda,0xc0,0xc7,0x95,0xd6,0xd4,0xd8,0xd0,0xc7,0xd4,0x95,0xdc,0xdb,0x95,0xcc,0xda,0xc0,0xc7,0x95,0xdc,0xe5,0xdd,0xda,0xdb,0xd0,0x92,0xc6,0x95,0x92,0xe6,0xd0,0xc1,0xc1,0xdc,0xdb,0xd2,0xc6,0x98,0xe5,0xc7,0xdc,0xc3,0xd4,0xd6,0xcc,0x98,0xf6,0xd4,0xd8,0xd0,0xc7,0xd4,0x92,0x95,0xda,0xc5,0xc1,0xdc,0xda,0xdb]

private func bodyTarget(environment num: UInt8) -> UInt8 {
    return num ^ 181
}

/*: "getPhotoStatus" :*/
fileprivate let controllerLineCount:String = "getPhotto of top object"
fileprivate let user_capsuleId:[Character] = ["o","S","t","a","t","u","s"]

/*: ' to access your album in your iPhone's 'Settings-Privacy-Album' option" :*/
fileprivate let helperToolString:[UInt8] = [0x8,0xf,0x5b,0x40,0xf,0x4e,0x4c,0x4c,0x4a,0x5c,0x5c,0xf,0x56,0x40,0x5a,0x5d,0xf,0x4e,0x43,0x4d,0x5a,0x42,0xf,0x46,0x41,0xf,0x56,0x40,0x5a,0x5d,0xf,0x46,0x7f,0x47,0x40,0x41,0x4a,0x8,0x5c,0xf,0x8,0x7c,0x4a,0x5b,0x5b,0x46,0x41,0x48,0x5c,0x2,0x7f,0x5d,0x46,0x59,0x4e,0x4c,0x56,0x2,0x6e,0x43,0x4d,0x5a,0x42,0x8,0xf,0x40,0x5f,0x5b,0x46,0x40,0x41]

private func verticalThe(full num: UInt8) -> UInt8 {
    return num ^ 47
}

/*: "getMicStatus" :*/
fileprivate let mainLaunchSystemMsg:String = "getMicSradio of"
fileprivate let userDirectPath:String = "tatucountry"

/*: ' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option" :*/
fileprivate let constAlbumStatus:[UInt8] = [0x34,0x33,0x67,0x7c,0x33,0x72,0x70,0x70,0x76,0x60,0x60,0x33,0x6a,0x7c,0x66,0x61,0x33,0x7e,0x7a,0x70,0x61,0x7c,0x63,0x7b,0x7c,0x7d,0x76,0x33,0x7a,0x7d,0x33,0x6a,0x7c,0x66,0x61,0x33,0x7a,0x43,0x7b,0x7c,0x7d,0x76,0x34,0x60,0x33,0x34,0x40,0x76,0x67,0x67,0x7a,0x7d,0x74,0x60,0x3e,0x43,0x61,0x7a,0x65,0x72,0x70,0x6a,0x3e,0x5e,0x7a,0x70,0x61,0x7c,0x63,0x7b,0x7c,0x7d,0x76,0x34,0x33,0x7c,0x63,0x67,0x7a,0x7c,0x7d]

/*: "Cancel" :*/
fileprivate let loggerGroupActionProtectionId:[Character] = ["C","a","n","c","e","l"]

/*: "Poor network, loading failed" :*/
fileprivate let formatterNeedMsg:String = "push or native remotePoor n"
fileprivate let engineMirrorString:String = "k, loavisible no cancel feedback if"
fileprivate let kNonachievementPath:[Character] = ["l","e","d"]

/*: "Refresh" :*/
fileprivate let user_scriptState:String = "Refrepush presentation"
fileprivate let dataFeedbackThatCount:String = "slabel"

/*: "Alert" :*/
fileprivate let managerPermitKey:String = "Alertself error"

/*: "OK" :*/
fileprivate let loggerHandleName:[Character] = ["O","K"]

/*: "Done" :*/
fileprivate let networkClickFlag:[Character] = ["D","o","n","e"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let networkYourFromStatus:[UInt8] = [0xfe,0xc2,0xc2,0xc6,0xe2,0xd9,0xd9,0xda,0x98,0xf8,0xd7,0xc2,0xdf,0xc0,0xd3,0xe2,0xd9,0xfc,0xc5,0x9e,0x91,0xc4,0xd3,0xd5,0xde,0xd7,0xc4,0xd1,0xd3,0x91,0x9f]

private func areaLinkDeadline(empty num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "onPageShow" :*/
fileprivate let cacheFeedbackFormat:String = "failure count resultonPag"
fileprivate let managerNamesOkKey:[Character] = ["w"]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let servicePedagogyId:[UInt8] = [0x3b,0x29,0x28,0x77,0x6f,0x68,0x53,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x77,0x6f,0x68,0x53,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "onPageHide" :*/
fileprivate let serviceNeverID:[Character] = ["o","n","P","a","g","e","H","i","d"]
fileprivate let configRequestDate:String = "option"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let parserGoMsg:[UInt8] = [0x3b,0x29,0x28,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ViewController.swift
//  OverseaH5
//
//  Created by DouXiu on 2025/9/23.
//

//: import UIKit
import UIKit
//: import WebViewJavascriptBridge
import WebViewJavascriptBridge
//: import WebKit
import WebKit

//: class AppWebViewController: UIViewController {
class PropertyViewController: UIViewController {
    
    //: var urlString: String = ""
    var urlString: String = ""
    /// 是否背景透明
    //: var clearBgColor = false
    var clearBgColor = false
    /// 是否全屏
    //: var fullscreen = true
    var fullscreen = true
    
    //: private var bridge: WebViewJavascriptBridge?
    private var bridge: WebViewJavascriptBridge?
    
    // Pending JS dialog completion handlers (ensure always-called to avoid WKWebView crash)
    //: private var pendingAlertCompletion: (() -> Void)?
    private var pendingAlertCompletion: (() -> Void)?
    //: private var pendingConfirmCompletion: ((Bool) -> Void)?
    private var pendingConfirmCompletion: ((Bool) -> Void)?
    //: private var pendingPromptCompletion: ((String?) -> Void)?
    private var pendingPromptCompletion: ((String?) -> Void)?

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []
        //: let userControl = WKUserContentController()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: let w = WKWebView(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: w.scrollView.contentInsetAdjustmentBehavior = .never
        w.scrollView.contentInsetAdjustmentBehavior = .never
        //: w.isOpaque = false
        w.isOpaque = false
        //: w.scrollView.bounces = false
        w.scrollView.bounces = false
        //: w.scrollView.alwaysBounceVertical = false
        w.scrollView.alwaysBounceVertical = false
        //: w.scrollView.alwaysBounceHorizontal = false
        w.scrollView.alwaysBounceHorizontal = false
        //: return w
        return w
    //: }()
    }()

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.addSubview(self.webView)
        view.addSubview(self.webView)
        //: var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        var frame = CGRect(origin: CGPoint.zero, size: UIScreen.main.bounds.size)
        //: if fullscreen == false {
        if fullscreen == false {
            //: frame.origin.y = AppConfig.getStatusBarHeight()
            frame.origin.y = MakeTap.source()
        }
        //: self.webView.frame = frame
        self.webView.frame = frame
 
        //: self.addBridgeMethod()
        self.disappear()
        //: self.beginStartRequest()
        self.center()
 
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(thatGatefoldMap),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }
    
    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        thatGatefoldMap()
    }
    
    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        alongPurchase()
        //: finalizePendingJSHandlersIfNeeded()
        nameKind()
    }

    //: deinit {
    deinit {
        //: removeBridgeMethod()
        methodOfChoice()
        //: finalizePendingJSHandlersIfNeeded()
        nameKind()
    }

    /// 发起网页请求
    //: private func beginStartRequest() {
    private func center() {
        //: if let url = URL(string: urlString) {
        if let url = URL(string: urlString) {
            //: let urlRequest = URLRequest(url: url)
            let urlRequest = URLRequest(url: url)
            //: self.webView.load(urlRequest)
            self.webView.load(urlRequest)
            //: self.clearJSBgColor()
            self.page()
        }
    }
    
    /// 设置页面为透明
    //: private func clearJSBgColor() {
    private func page() {
        //: guard clearBgColor == true else { return }
        guard clearBgColor == true else { return }
        //: webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
        webView.evaluateJavaScript(String(bytes: cacheHeadString.map{$0^74}, encoding: .utf8)!) { _, _  in
        }
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: webView.backgroundColor = .clear
        webView.backgroundColor = .clear
        //: webView.scrollView.backgroundColor = .clear
        webView.scrollView.backgroundColor = .clear
        //: webView.scrollView.bounces = false
        webView.scrollView.bounces = false
        //: webView.scrollView.alwaysBounceVertical = false
        webView.scrollView.alwaysBounceVertical = false
        //: webView.scrollView.alwaysBounceHorizontal = false
        webView.scrollView.alwaysBounceHorizontal = false
        //: webView.isOpaque = false
        webView.isOpaque = false
    }
    
    /// 关闭webview事件
    //: func closeWeb() {
    func pack() {
        //: if webView.canGoBack {
        if webView.canGoBack {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        
        //: removeBridgeMethod()
        methodOfChoice()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = AppConfig.currentViewController() {
                if let currentVC = MakeTap.mopUp() {
                    //: if currentVC.isKind(of: AppWebViewController.self) {
                    if currentVC.isKind(of: PropertyViewController.self) {
                        //: (currentVC as! AppWebViewController).jsEvent_onPageShow()
                        (currentVC as! PropertyViewController).thatGatefoldMap()
                    }
                }
            }
        }
    }
}

//: extension AppWebViewController: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
extension PropertyViewController: WKScriptMessageHandler, WebViewJavascriptBridgeBaseDelegate {
    //: func _evaluateJavascript(_ javascriptCommand: String!) -> String! {
    func _evaluateJavascript(_ javascriptCommand: String!) -> String! {
        //: return ""
        return ""
    }

    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        //: print("js call method name = \(message.name), message = \(message.body)")
        // 兼容老事件
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: let type = message.name
            let type = message.name
            //: if type == "closeWeb" {
            if type == (String(engineOriginPath)) {
                //: self.closeWeb()
                self.pack()
                
            //: } else if type == "toUrl" {
            } else if type == (String(mainBlackState.prefix(5))) {
                //: if let url = message.body as? String {
                if let url = message.body as? String {
                    //: AppWebViewController.openNewWebView(url)
                    PropertyViewController.decision(url)
                }
            }
        }
    }

    //: func addBridgeMethod() {
    func disappear() {
        //: self.bridge = WebViewJavascriptBridge(self.webView)
        self.bridge = WebViewJavascriptBridge(self.webView)
        //: self.bridge?.setWebViewDelegate(self)
        self.bridge?.setWebViewDelegate(self)
        //: self.bridge?.registerHandler("syncAppInfo", handler: { data, callback in
        self.bridge?.registerHandler((String(loggerBackwashPath) + String(userHandleMinimumDate)), handler: { data, callback in
            //: print("js call getUserIdFromObjC, data from js is %@", data as Any)
            //: if callback != nil {
            if callback != nil {
                //: if let dic = data as? [String: Any] {
                if let dic = data as? [String: Any] {
                    //: self.handleH5Message(schemeDic: dic) { backDic in
                    self.user(schemeDic: dic) { backDic in
                        //: callback?(backDic)
                        callback?(backDic)
                        //: DispatchQueue.main.async {
                        DispatchQueue.main.async {
                            //: self.handAuthOpenURL(dic: backDic)
                            self.doingAdd(dic: backDic)
                        }
                    }
                }
            }
        //: })
        })
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "closeWeb")
        ucController.add(ShouldProcess(self), name: (String(engineOriginPath)))
        //: ucController.add(AppWebViewScriptDelegateHandler(self), name: "toUrl")
        ucController.add(ShouldProcess(self), name: (String(mainBlackState.prefix(5))))
    }

    //: func removeBridgeMethod() {
    func methodOfChoice() {
        //: let ucController = self.webView.configuration.userContentController
        let ucController = self.webView.configuration.userContentController
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            //: ucController.removeAllScriptMessageHandlers()
            ucController.removeAllScriptMessageHandlers()
        //: } else {
        } else {
            //: ucController.removeScriptMessageHandler(forName: "closeWeb")
            ucController.removeScriptMessageHandler(forName: (String(engineOriginPath)))
            //: ucController.removeScriptMessageHandler(forName: "toUrl")
            ucController.removeScriptMessageHandler(forName: (String(mainBlackState.prefix(5))))
        }
    }

    //: func handAuthOpenURL(dic: [String: Any]) {
    func doingAdd(dic: [String: Any]) {
        //: if let typeName = dic["typeName"] as? String, let isAuth = dic["isAuth"] as? Bool, let isFirst = dic["isFirst"] as? Bool {
        if let typeName = dic[(String(factoryLinkRemoteVersion.prefix(5)) + notiPropertyTitle.replacingOccurrences(of: "net", with: "a"))] as? String, let isAuth = dic[(String(showUserID))] as? Bool, let isFirst = dic[(String(formatterPageFormat.suffix(7)))] as? Bool {
            //: if isAuth || isFirst {
            if isAuth || isFirst {
                //: return
                return
            }
            //: var message = "Please click 'Go' to allow access"
            var message = String(bytes: serviceManagerUrl.reversed(), encoding: .utf8)!
            //: var needAlert = false
            var needAlert = false
            //: if typeName == "getCameraStatus" {
            if typeName == (String(modelPadStatus.prefix(6)) + "eraSt" + String(modelChoiceId)) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your camera in your iPhone's 'Settings-Privacy-Camera' option"
                message = (String(notiApplicationProcessStr.prefix(9)) + String(showPreviousList)) + "\(configWarnUsedProgressTitle)" + String(bytes: controllerOkValue.map{bodyTarget(environment: $0)}, encoding: .utf8)!
                
            //: } else if typeName == "getPhotoStatus" {
            } else if typeName == (String(controllerLineCount.prefix(7)) + String(user_capsuleId)) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your album in your iPhone's 'Settings-Privacy-Album' option"
                message = (String(notiApplicationProcessStr.prefix(9)) + String(showPreviousList)) + "\(configWarnUsedProgressTitle)" + String(bytes: helperToolString.map{verticalThe(full: $0)}, encoding: .utf8)!
                
            //: } else if typeName == "getMicStatus" {
            } else if typeName == (String(mainLaunchSystemMsg.prefix(7)) + userDirectPath.replacingOccurrences(of: "country", with: "s")) {
                //: needAlert = true
                needAlert = true
                //: message = "Please allow '\(AppName)' to access your microphone in your iPhone's 'Settings-Privacy-Microphone' option"
                message = (String(notiApplicationProcessStr.prefix(9)) + String(showPreviousList)) + "\(configWarnUsedProgressTitle)" + String(bytes: constAlbumStatus.map{$0^19}, encoding: .utf8)!
            }

            //: if needAlert {
            if needAlert {
                //: let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)
                let alertController = UIAlertController(title: nil, message: message, preferredStyle: .alert)

                //: let action1 = UIAlertAction(title: "Cancel", style: .default) { _ in
                let action1 = UIAlertAction(title: (String(loggerGroupActionProtectionId)), style: .default) { _ in
                }
                //: let action2 = UIAlertAction(title: "Go", style: .destructive) { _ in
                let action2 = UIAlertAction(title: "Go", style: .destructive) { _ in
                    //: if let url = URL(string: UIApplication.openSettingsURLString) {
                    if let url = URL(string: UIApplication.openSettingsURLString) {
                        //: UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                        UIApplication.shared.open(url, options: [:], completionHandler: { _ in })
                    }
                }
                //: alertController.addAction(action1)
                alertController.addAction(action1)
                //: alertController.addAction(action2)
                alertController.addAction(action2)
                //: present(alertController, animated: true)
                present(alertController, animated: true)
            }
        }
    }
}

//: extension AppWebViewController: WKNavigationDelegate, WKUIDelegate {
extension PropertyViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        //: clearJSBgColor()
        page()
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        //: let alertController = UIAlertController(title: nil, message: "Poor network, loading failed", preferredStyle: .alert)
        let alertController = UIAlertController(title: nil, message: (String(formatterNeedMsg.suffix(6)) + "etwor" + String(engineMirrorString.prefix(6)) + "ding fai" + String(kNonachievementPath)), preferredStyle: .alert)
        //: let action = UIAlertAction(title: "Refresh", style: .default) { _ in
        let action = UIAlertAction(title: (String(user_scriptState.prefix(5)) + dataFeedbackThatCount.replacingOccurrences(of: "label", with: "h")), style: .default) { _ in
            //: self.reloadWebView()
            self.rating()
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: present(alertController, animated: true)
        present(alertController, animated: true)
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
    }

    //: func reloadWebView() {
    func rating() {
        //: if self.webView.url != nil {
        if self.webView.url != nil {
            //: self.webView.reload()
            self.webView.reload()
        //: } else {
        } else {
            //: self.beginStartRequest()
            self.center()
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {}
    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {}

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
            //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

    //: func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
    func webViewWebContentProcessDidTerminate(_ webView: WKWebView) {
        //: self.reloadWebView()
        self.rating()
    }

    //: func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
        //: pendingAlertCompletion = completionHandler
        pendingAlertCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(managerPermitKey.prefix(5))), message: message, preferredStyle: .alert)
        //: let action = UIAlertAction(title: "OK", style: .default) { _ in
        let action = UIAlertAction(title: "OK", style: .default) { _ in
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
        //: alertController.addAction(action)
        alertController.addAction(action)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = MakeTap.mopUp() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
        //: } else {
        } else {
            // Fallback to avoid crash if cannot present
            //: self.pendingAlertCompletion?()
            self.pendingAlertCompletion?()
            //: self.pendingAlertCompletion = nil
            self.pendingAlertCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: pendingConfirmCompletion = completionHandler
        pendingConfirmCompletion = completionHandler
        //: let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(managerPermitKey.prefix(5))), message: message, preferredStyle: .alert)
        //: let okAction = UIAlertAction(title: "OK", style: .default) { _ in
        let okAction = UIAlertAction(title: (String(loggerHandleName)), style: .default) { _ in
            //: self.pendingConfirmCompletion?(true)
            self.pendingConfirmCompletion?(true)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(loggerGroupActionProtectionId)), style: .cancel) { _ in
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(okAction)
        alertController.addAction(okAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = MakeTap.mopUp() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
        //: } else {
        } else {
            // Fallback default = false
            //: self.pendingConfirmCompletion?(false)
            self.pendingConfirmCompletion?(false)
            //: self.pendingConfirmCompletion = nil
            self.pendingConfirmCompletion = nil
        }
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: pendingPromptCompletion = completionHandler
        pendingPromptCompletion = completionHandler
        //: let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let doneAction = UIAlertAction(title: "Done", style: .default) { _ in
        let doneAction = UIAlertAction(title: (String(networkClickFlag)), style: .default) { _ in
            //: let text = alertController.textFields?.first?.text
            let text = alertController.textFields?.first?.text
            //: self.pendingPromptCompletion?(text)
            self.pendingPromptCompletion?(text)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: let cancelAction = UIAlertAction(title: "Cancel", style: .cancel) { _ in
        let cancelAction = UIAlertAction(title: (String(loggerGroupActionProtectionId)), style: .cancel) { _ in
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
        //: alertController.addAction(cancelAction)
        alertController.addAction(cancelAction)
        //: alertController.addAction(doneAction)
        alertController.addAction(doneAction)
        //: if let topVC = AppConfig.currentViewController() {
        if let topVC = MakeTap.mopUp() {
            //: topVC.present(alertController, animated: true)
            topVC.present(alertController, animated: true)
        //: } else {
        } else {
            // Fallback default = nil
            //: self.pendingPromptCompletion?(nil)
            self.pendingPromptCompletion?(nil)
            //: self.pendingPromptCompletion = nil
            self.pendingPromptCompletion = nil
        }
    }

    //: @available(iOS 15.0, *)
    @available(iOS 15.0, *)
    //: func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
    func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
        //: decisionHandler(.grant)
        decisionHandler(.grant)
    }
}

//: extension AppWebViewController {
extension PropertyViewController {
    /// Ensure any pending JS dialog completion handlers are executed to avoid WKWebView crash
    //: private func finalizePendingJSHandlersIfNeeded() {
    private func nameKind() {
        //: if let alertCompletion = pendingAlertCompletion {
        if let alertCompletion = pendingAlertCompletion {
            //: alertCompletion()
            alertCompletion()
            //: pendingAlertCompletion = nil
            pendingAlertCompletion = nil
        }
        //: if let confirmCompletion = pendingConfirmCompletion {
        if let confirmCompletion = pendingConfirmCompletion {
            //: confirmCompletion(false)
            confirmCompletion(false)
            //: pendingConfirmCompletion = nil
            pendingConfirmCompletion = nil
        }
        //: if let promptCompletion = pendingPromptCompletion {
        if let promptCompletion = pendingPromptCompletion {
            //: promptCompletion(nil)
            promptCompletion(nil)
            //: pendingPromptCompletion = nil
            pendingPromptCompletion = nil
        }
    }
    
    /// 通知三方H5刷新金币
    //: func third_jsEvent_refreshCoin() {
    func info() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: networkYourFromStatus.map{areaLinkDeadline(empty: $0)}, encoding: .utf8)!) { data, error in
        }
    }
    
    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func thatGatefoldMap() {
        //: self.bridge?.callHandler("onPageShow")
        self.bridge?.callHandler((String(cacheFeedbackFormat.suffix(5)) + "eSho" + String(managerNamesOkKey)))
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: servicePedagogyId.reversed(), encoding: .utf8)!) { data, error in
            //: print("jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
        }
    }
    
    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func alongPurchase() {
        //: self.bridge?.callHandler("onPageHide")
        self.bridge?.callHandler((String(serviceNeverID) + configRequestDate.replacingOccurrences(of: "option", with: "e")))
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: parserGoMsg.reversed(), encoding: .utf8)!) { data, error in
            //: print("jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
        }
    }
}